# === Lark 应用凭证 ===
APP_ID=cli_xxx_your_app_id
APP_SECRET=your_app_secret

# 机器人在 Lark 中的显示名称（群聊 @ 检测依赖它，务必与显示名一致）
BOT_NAME=Skygpt

# === 数据库&缓存（Railway 插件提供的连接串）===
# Railway Postgres 插件会给 postgresql://...；程序会自动转换为 async 驱动
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DBNAME

# Railway Redis/Upstash 提供的 redis:// 连接串（用于缓存 tenant_access_token）
REDIS_URL=redis://default:PASSWORD@HOST:PORT

# === OpenAI（可选：用于对话/摘要）===
OPENAI_API_KEY=sk-xxxxxx

# === 应用设置 ===
TIMEZONE=Asia/Taipei
LOG_LEVEL=INFO

# Lark API 基域：
# 国际版 → https://open.larksuite.com
# 中国大陆租户（飞书）→ https://open.feishu.cn
LARK_BASE=https://open.larksuite.com

# 群聊是否必须 @ 机器人才响应（True/False）
# 调试期可设为 False，验证通过后建议改回 True
REQUIRE_MENTION=True
